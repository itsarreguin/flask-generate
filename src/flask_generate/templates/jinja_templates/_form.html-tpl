<form action="{{ endpoint }}" method="post">
  {{ form.csrf_token() }}
  [%- for field in fields %]
  <div>
    {{ form.[[ field_name(field) ]].label() }}
    {{ form.[[ field_name(field) ]]() }}
  </div>
  [%- endfor %]
  <div>
    {{ form.submit() }}
  </div>
</form>