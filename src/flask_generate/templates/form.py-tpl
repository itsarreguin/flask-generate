from flask_wtf import FlaskForm
from wtforms import fields, widgets


class {{ form_name }}Form(FlaskForm):

    {% for field in fields -%}
        {{ field_name(field) }} = fields.{{ form_field(field) }}
    {% endfor %}
    submit = fields.SubmitField('submit')