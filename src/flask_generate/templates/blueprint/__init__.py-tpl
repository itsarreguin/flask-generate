from flask import Blueprint


path = Blueprint('[[ blueprint_name ]]', __name__, template_folder='template', url_prefix='/[[ blueprint_name ]]')

from [[ app_name ]].[[ blueprint_name ]] import routes as routes