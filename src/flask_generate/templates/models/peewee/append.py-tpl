class [[ model_name ]](Model):

    [% for field in fields -%]
        [[ field_name(field) ]] = [[ table_field(orm, field) ]]
    [% endfor -%]
    created_at = DateTimeField()
    updated_at = DateTimeField()

    class Meta:
        database = database